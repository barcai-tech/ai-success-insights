/**
 * Playbooks Page (Server Component)
 * Renders the PlaybooksClient component for displaying playbook library
 */

import PlaybooksClient from "@/components/features/playbooks/PlaybooksClient";

export default function PlaybooksPage() {
  return <PlaybooksClient />;
}
