/**
 * Upload Page (Server Component)
 * Renders the UploadClient component for CSV file uploads
 */

import UploadClient from "@/components/features/upload/UploadClient";

export default function UploadPage() {
  return <UploadClient />;
}
